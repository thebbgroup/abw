<!DOCTYPE html>
<!--[if lt IE 7]><html class="no-js lt-ie9 lt-ie8 lt-ie7" lang="en"><![endif]-->
<!--[if IE 7]><html class="no-js lt-ie9 lt-ie8" lang="en"><![endif]-->
<!--[if IE 8]><html class="no-js lt-ie9" lang="en"><![endif]-->
<!--[if gt IE 8]<!--><html class="no-js" lang="en"><!--<![endif]-->
<head>
    {# replaces "no-js" class with "js" in the <html> tag if js enabled #}
    <script>!function(h){h.className=h.className.replace(/\bno-js\b/,'js')}(document.documentElement)</script>
    <meta charset="utf-8">
    <title>{% block title %}Anti-Bullying Week{% endblock %}</title>
    {% assets 'favicon' %}
        <link rel="shortcut icon" href="{{ ASSET_URL }}" type="image/x-icon">
    {% endassets %}
    {% assets 'css_main' %}
        <link rel="stylesheet" href="{{ ASSET_URL }}">
    {% endassets %}
    {% assets 'js_head' %}
        <script src="{{ ASSET_URL }}"></script>
    {% endassets %}
</head>
<body{% block body_attr %}{% endblock %}>
    {% include '_includes/google-analytics.jinja' %}
    {% with messages = get_flashed_messages(with_categories) %}
    <ul id="flash">
        {% for msg in messages %}
        <li class="err">{{ msg }}</li>
        {% endfor %}
    </ul>
    {% endwith %}
    {% block body %}{% endblock %}
    {% assets 'js_foot' %}
        <script src="{{ ASSET_URL }}"></script>
    {% endassets %}
</body>
</html>
